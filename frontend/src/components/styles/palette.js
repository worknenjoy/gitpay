import { indigo, teal, grey } from '@material-ui/core/colors'

const Palette = {
  palette: {
    primary: {
      light: teal[50],
      main: teal[500],
      dark: teal[700],
      contrastText: grey[100]
    },
    secondary: indigo
  }
}

export default Palette
