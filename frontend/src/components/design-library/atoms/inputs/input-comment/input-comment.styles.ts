import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  counter: {
    fontFamily: 'Roboto',
    color: '#a9a9a9',
    marginTop: '10px',
    textAlign: 'right'
  }
}))

export default useStyles
