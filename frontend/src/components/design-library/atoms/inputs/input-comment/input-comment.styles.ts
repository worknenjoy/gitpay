import { styled } from '@mui/material/styles'

const useStyles = styled(() => ({
  counter: {
    fontFamily: 'Roboto',
    color: '#a9a9a9',
    marginTop: '10px',
    textAlign: 'right'
  }
}))

export default useStyles
