import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  termsLabel: {
    paddingTop: 0,
  },
  checkbox: {
    paddingRight: 5,
  },
}))

export default useStyles
