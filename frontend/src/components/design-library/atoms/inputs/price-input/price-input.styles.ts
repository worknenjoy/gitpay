import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  formPayment: {
    marginTop: 10,
    marginBottom: 10
  },
  currencySymbol: {
    fontSize: 28
  },
  input: {
    fontSize: 42,
    fontWeight: 'bold',
    textAlign: 'right',
    height: 98
  }
}))

export default useStyles
