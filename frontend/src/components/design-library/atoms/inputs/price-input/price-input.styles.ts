import { styled } from '@mui/material/styles'

const useStyles = styled(() => ({
  formPayment: {
    marginTop: 10,
    marginBottom: 10
  },
  currencySymbol: {
    fontSize: 28
  },
  input: {
    fontSize: 42,
    fontWeight: 'bold',
    textAlign: 'right',
    height: 98
  }
}))

export default useStyles
