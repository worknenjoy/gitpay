import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(() => ({
  buttonMargin: {
    marginRight: 10
  },
  textMargin: {
    marginLeft: 10
  },
  linkMargin: {
    display: 'inline-box',
    marginLeft: 5
  }
}))

export default useStyles
