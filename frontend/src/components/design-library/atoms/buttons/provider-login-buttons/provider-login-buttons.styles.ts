import { styled } from '@mui/material/styles'

const useStyles = styled(() => ({
  buttonMargin: {
    marginRight: 10
  },
  textMargin: {
    marginLeft: 10
  },
  linkMargin: {
    display: 'inline-box',
    marginLeft: 5
  }
}))

export default useStyles
