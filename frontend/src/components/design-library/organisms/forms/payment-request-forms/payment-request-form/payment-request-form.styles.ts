import { styled } from '@mui/material/styles'

export const AlertWrapper = styled('div')(({ theme }) => ({
  marginBottom: 20,
  marginTop: 20
}))

export const AlertInner = styled('div')(({ theme }) => ({
  marginBottom: 20
}))

export const EndAdornment = styled('div')(({ theme }) => ({
  marginLeft: 8
}))
